# Test set (benign side)

The benchmark's benign variants come from gnomAD and are included here as `test_set_gnomad_benign.tsv` (1,166 variants with their INDELVAR scores). The pathogenic variants come from HGMD and are licensed, so they are not included; the full benchmark is reported in the paper.
